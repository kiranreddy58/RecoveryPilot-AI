version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - .env.example
    volumes:
      - ./backend:/app
    # depends_on:
    #   - postgres
    #   - redis

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

  # postgres:
  #   image: postgres:15
  #   environment:
  #     POSTGRES_USER: user
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: recoverypilot
  #   ports:
  #     - "5432:5432"

  # redis:
  #   image: redis:7
  #   ports:
  #     - "6379:6379"
